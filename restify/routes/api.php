<?php

declare(strict_types=1);

use Restify\Routing\Router;

return static function (Router $router): void {
    // Define API specific routes here, e.g.:
    // $router->get('/api/ping', static fn () => ['ok' => true]);
};
